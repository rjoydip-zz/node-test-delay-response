{
  "name": "node-test-delay-response",
  "version": "1.0.0",
  "description": "Test a api which returns a value after some delay",
  "main": "app.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "tap \"test/**/*.test.js\"",
    "start": "fastify start -l info app.js",
    "client": "node client.js",
    "dev": "fastify start -w -l info -P --ignore-watch client app.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "fastify": "^3.0.0",
    "fastify-autoload": "^3.3.1",
    "fastify-cli": "^2.12.0",
    "fastify-plugin": "^3.0.0",
    "fastify-sensible": "^3.1.0",
    "node-fetch": "^2.6.1",
    "p-forever": "^3.0.1"
  },
  "devDependencies": {
    "tap": "^15.0.9"
  }
}
